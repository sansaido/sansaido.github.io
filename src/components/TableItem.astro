---
interface Props {
    label: string,
    content?: string,
    color?: "red" | "blue" | "grey" | "bronze",
    className?: string,
    paddingBottom?: Number
}

const {label, content, color = "red", className, paddingBottom = 10} = Astro.props

const setColorClasses = (color:string) => {
    if(color === "red") {
        return "bg-red text-white"
    } else if (color=== "blue") {
        return "bg-blue-lt text-black"
    } else if (color=== "grey") {
        return "bg-grey text-black"
    } else if (color=== "bronze") {
        return "bg-bronze-fb text-white"
    }
}

const colorClasses = setColorClasses(color)
const pbClasses = {
    4: 'pb-4',
    10: 'pb-10'
}

const paddingBottomClass = pbClasses[paddingBottom]
---

<dl class={`flex items-start gap-2 md:gap-6 sm:gap-10 ${paddingBottomClass} ${className && className}`}>
    <dt
        class:list={[colorClasses, ["text-sm md:text-lg rounded-full height-fit px-2 md:px-4 py-1 md:py-2 min-w-19 md:min-w-24 flex justify-center"]]}
        set:html={label}
    />
    <dd set:html={content} class="self-center"> 
        <slot/>
    </dd>
</dl>

<style>
</style>